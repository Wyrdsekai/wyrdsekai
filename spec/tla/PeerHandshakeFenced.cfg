\* Add statements after this line.
\* TLC config for PeerHandshakeFenced (the gap-#2/#3 fix demonstration).
\*
\* Same handshake as PeerHandshake, plus a monotonic epoch fence on every
\* transition. The claim: the fence makes NoHalfOpenLink + NoLostRevoke hold
\* as PURE SAFETY INVARIANTS, with NO reconcile loop in the spec — i.e. the
\* handshake is self-consistent on the crossing-message path, where the bare
\* PeerHandshake needed the F6 reconcile to converge.
SPECIFICATION Spec
CONSTANT MaxMsgs  = 2
CONSTANT MaxEpoch = 4
INVARIANT TypeOK
INVARIANT NoHalfOpenLink
INVARIANT NoLostRevoke
CHECK_DEADLOCK FALSE
