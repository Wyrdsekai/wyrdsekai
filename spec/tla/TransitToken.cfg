\* Add statements after this line.
\* TLC config for TransitToken (P1 Deliverable A).
\*
\* Committed gate = the RELIABLE channel (no drop, no redeliver): the happy-path
\* relocate handoff is single-owner correct. Green.
\*
\* FINDINGS (FINDINGS.md): set AllowDrop = TRUE to reproduce a NoLoss violation
\* (the companion is stopped at source and the transit token is dropped → hosted
\* nowhere), and AllowRedeliver = TRUE to reproduce a NoDuplication violation (a
\* redelivered token spawns a second host → companion in two zones at once, the
\* #492 EntityLeft-duplication family). Left FALSE so CI is green while the gaps
\* are recorded.
SPECIFICATION Spec
CONSTANT MaxMsgs       = 3
CONSTANT AllowDrop      = FALSE
CONSTANT AllowRedeliver = FALSE
INVARIANT TypeOK
INVARIANT NoDuplication
INVARIANT NoLoss
CHECK_DEADLOCK FALSE
