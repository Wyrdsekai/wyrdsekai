\* Add statements after this line.
\* TLC config for PrimaryFencing (P3 Deliverable A).
\*
\* Committed gate = NoStaleApply (+ TypeOK): GREEN unconditionally — the epoch
\* fence shipped in #114 correctly rejects a stale primary's writes.
\*
\* FINDING (FINDINGS.md): add INVARIANT NoDoubleApply and re-run with
\* DurableDedup = FALSE to reproduce a violation — a retry that straddles a
\* primary HANDOVER applies twice, because the new primary's in-memory dedupTable
\* (MutationRouter:90) starts empty and doesn't carry the prior key. Set
\* DurableDedup = TRUE (carry the dedup set in durable room state) to restore it.
SPECIFICATION Spec
CONSTANT MaxEpoch      = 3
CONSTANT MaxMsgs       = 3
CONSTANT MaxApply      = 3
CONSTANT DurableDedup  = FALSE
INVARIANT TypeOK
INVARIANT NoStaleApply
CHECK_DEADLOCK FALSE
