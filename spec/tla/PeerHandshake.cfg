\* TLC config for PeerHandshake (P0 spike Deliverable A).
\*
\* Committed gate = TypeOK (well-formedness). This proves the TLA+/TLC toolchain
\* + CI wiring end-to-end and stays green.
\*
\* The two relational invariants below are DOCUMENTED FINDINGS (see FINDINGS.md):
\* uncommenting NoHalfOpenLink reproduces a 9-state counterexample showing the
\* bare bilateral handshake can durably diverge into a half-open agreement
\* (one zone ACTIVE, the other NONE) absent the F6 reconcile loop. Left commented
\* so CI is green while the finding is recorded; revisit when we decide whether to
\* harden the handshake or treat reconcile as the convergence contract.
\*
\* CHECK_DEADLOCK is off: this is a TERMINATING handshake — quiescence at mutual
\* agreement (or mutual NONE) is correct, not a fault.
SPECIFICATION Spec
CONSTANT MaxMsgs = 2
INVARIANT TypeOK
\* INVARIANT NoHalfOpenLink   \* FINDING — see FINDINGS.md (reproduces a violation)
\* INVARIANT NoLostRevoke     \* companion safety property, same family
CHECK_DEADLOCK FALSE
