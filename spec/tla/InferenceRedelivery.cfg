\* Add statements after this line.
\* TLC config for InferenceRedelivery (P2 Deliverable A).
\*
\* Committed gate = the RELIABLE transport (AllowRedeliver = FALSE): the provider
\* serves each request exactly once. Green — matches today's core-NATS reality.
\*
\* FINDING (FINDINGS.md): set AllowRedeliver = TRUE with ProviderDedup = FALSE to
\* reproduce an AtMostOnceServed violation (a redelivered request re-runs inference
\* and publishes a second reply stream = the #264 duplication class, were the
\* transport ever at-least-once / JetStream). Set ProviderDedup = TRUE to show the
\* streamId-keyed dedup fix restores AtMostOnceServed even under redelivery.
SPECIFICATION Spec
CONSTANT MaxMsgs        = 3
CONSTANT AllowRedeliver = FALSE
CONSTANT ProviderDedup  = FALSE
INVARIANT TypeOK
INVARIANT AtMostOnceServed
CHECK_DEADLOCK FALSE
