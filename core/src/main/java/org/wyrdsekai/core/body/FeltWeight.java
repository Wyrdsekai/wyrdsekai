package org.wyrdsekai.core.body;

/**
 * How present a part is in her when it is fine, and how loud its loss is.
 *
 * <p>A camera in an unused room is quiet: its loss hurts once and is then a memory. The
 * record is loud: while it is gone nothing else matters, and the felt line says so every
 * turn.</p>
 */
public enum FeltWeight {
    QUIET, PRESENT, LOUD
}
