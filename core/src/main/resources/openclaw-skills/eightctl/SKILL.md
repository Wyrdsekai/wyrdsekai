---
name: eightctl
description: Control an Eight Sleep pod - temperature, alarm, side settings.
version: "1.0"
metadata:
  openclaw:
    requires:
      bins: [eightctl]
  wyrdsekai:
    room: hearth
---
Run the CLI below to do the work. Start with `--help` if you are unsure of the exact flags, then compose ONE precise command. Report the actual output honestly; if the command fails, say what failed instead of narrating success.

Binary: `eightctl`. Adjusting someone's bed temperature is intimate household control - only act on an explicit request from that person.
