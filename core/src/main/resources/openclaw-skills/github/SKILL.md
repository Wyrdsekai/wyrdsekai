---
name: github
description: Work with GitHub - issues, pull requests, repos - via the gh CLI.
version: "1.0"
metadata:
  openclaw:
    requires:
      bins: [gh]
  wyrdsekai:
    room: workshop
---
Run the CLI below to do the work. Start with `--help` if you are unsure of the exact flags, then compose ONE precise command. Report the actual output honestly; if the command fails, say what failed instead of narrating success.

Binary: `gh`. `gh issue list`, `gh pr view`, `gh pr checks` are safe reads. Creating issues/PRs or commenting is public, attributed writing - confirm the text first. Never force-push, close, or merge unless explicitly asked.
