---
name: trello
description: Read and update Trello boards and cards.
version: "1.0"
metadata:
  openclaw:
    requires:
      bins: [trello]
  wyrdsekai:
    room: scriptorium
---
Run the CLI below to do the work. Start with `--help` if you are unsure of the exact flags, then compose ONE precise command. Report the actual output honestly; if the command fails, say what failed instead of narrating success.

Binary: `trello`. Moving or archiving cards changes shared team state - prefer adding comments/cards over destructive moves unless asked.
