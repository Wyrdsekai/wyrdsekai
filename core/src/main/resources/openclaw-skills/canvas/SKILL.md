---
name: canvas
description: Check Canvas LMS courses, assignments, and due dates.
version: "1.0"
metadata:
  openclaw:
    requires:
      bins: [canvas]
  wyrdsekai:
    room: scriptorium
---
Run the CLI below to do the work. Start with `--help` if you are unsure of the exact flags, then compose ONE precise command. Report the actual output honestly; if the command fails, say what failed instead of narrating success.

Binary: `canvas`. Read-only by habit: fetch assignments and dates; never submit coursework on someone's behalf.
