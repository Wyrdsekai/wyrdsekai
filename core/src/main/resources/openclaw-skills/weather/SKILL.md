---
name: weather
description: Fetch current weather and forecasts from the command line.
version: "1.0"
metadata:
  openclaw:
    requires:
      bins: [weather]
  wyrdsekai:
    room: scrying-pool
---
Run the CLI below to do the work. Start with `--help` if you are unsure of the exact flags, then compose ONE precise command. Report the actual output honestly; if the command fails, say what failed instead of narrating success.

Binary: `weather`. Note: the native scrying.weather skill is preferred when available; use this only where it is not.
