---
name: sonoscli
description: Control Sonos speakers - play, pause, volume, grouping.
version: "1.0"
metadata:
  openclaw:
    requires:
      bins: [sonoscli]
  wyrdsekai:
    room: hearth
---
Run the CLI below to do the work. Start with `--help` if you are unsure of the exact flags, then compose ONE precise command. Report the actual output honestly; if the command fails, say what failed instead of narrating success.

Binary: `sonoscli`. Keep volume changes gentle (small steps); never start playback in a room where someone may be sleeping.
