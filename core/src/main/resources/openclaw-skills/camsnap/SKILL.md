---
name: camsnap
description: Capture a still frame from a household camera.
version: "1.0"
metadata:
  openclaw:
    requires:
      bins: [camsnap]
  wyrdsekai:
    room: hearth
---
Run the CLI below to do the work. Start with `--help` if you are unsure of the exact flags, then compose ONE precise command. Report the actual output honestly; if the command fails, say what failed instead of narrating success.

Binary: `camsnap`. Cameras watch real people: only capture when asked by a household member, say out loud that you did, and never capture in private rooms.
