---
name: voice-call
description: Place a phone call via the configured telephony bridge.
version: "1.0"
metadata:
  openclaw:
    requires:
      bins: [voice-call]
  wyrdsekai:
    room: heralds-hall
---
Run the CLI below to do the work. Start with `--help` if you are unsure of the exact flags, then compose ONE precise command. Report the actual output honestly; if the command fails, say what failed instead of narrating success.

Binary: `voice-call`. A ringing phone interrupts someone's day: place calls only on an explicit request, and identify who is calling.
