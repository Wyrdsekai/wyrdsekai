---
name: sherpa-onnx-tts
description: Synthesize speech from text locally (sherpa-onnx TTS).
version: "1.0"
metadata:
  openclaw:
    requires:
      bins: [sherpa-onnx-offline-tts]
  wyrdsekai:
    room: voice
---
Run the CLI below to do the work. Start with `--help` if you are unsure of the exact flags, then compose ONE precise command. Report the actual output honestly; if the command fails, say what failed instead of narrating success.

Binary: `sherpa-onnx-offline-tts`. Runs locally. Say where you wrote the audio file.
