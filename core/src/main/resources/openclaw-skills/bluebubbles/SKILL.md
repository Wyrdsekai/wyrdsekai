---
name: bluebubbles
description: Send and read iMessage via a BlueBubbles server.
version: "1.0"
metadata:
  openclaw:
    requires:
      bins: [imsg]
  wyrdsekai:
    room: heralds-hall
---
Run the CLI below to do the work. Start with `--help` if you are unsure of the exact flags, then compose ONE precise command. Report the actual output honestly; if the command fails, say what failed instead of narrating success.

Binary: `imsg`. Messages go to real people - confirm the recipient and exact text before sending.
